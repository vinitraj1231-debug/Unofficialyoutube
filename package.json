{
  "name": "unofficialyoutube",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.js --bundle --outfile=dist/index.js --target=es2022 --format=esm",
    "test": "node test_routes.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "wrangler": "^3.90.0"
  }
}
