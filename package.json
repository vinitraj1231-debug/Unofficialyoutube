{
  "name": "unofficialyoutube",
  "version": "1.0.0",
  "description": "Unofficial YouTube InnerTube API wrapper for Cloudflare Workers",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "node test_routes.js",
    "build": "echo 'Build complete'"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "wrangler": "^3.90.0"
  }
}
